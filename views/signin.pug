html(lang="en")
    head
        meta(charset="UTF-8")
        script(src="/js/signinButton.js" defer) 
        title signin
    body
        #signUpSection
            input#usernameInput(type="text", placeholder="Username")
            input#passwordInput(type="password", placeholder="Password (8+ characters)")
            button#signInButton Sign in