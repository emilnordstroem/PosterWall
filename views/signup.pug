html(lang="en")
    head
        meta(charset="UTF-8")
        script(src="/js/signupButton.js" defer) 
        title signup
    body
        #signUpSection
            input#usernameInput(type="text", placeholder="Username")
            input#passwordInput(type="password", placeholder="Password (8+ characters)")
            button#signUpButton Sign up
        #signInReference
            p Already a user?
            button#signInLinkButton Sign in 